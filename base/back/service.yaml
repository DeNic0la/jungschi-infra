apiVersion: v1
kind: Service
metadata:
  name: jungschi-back
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    service.kubernetes.io/service-proxy-name: "jungschi-back"
spec:
  selector:
    app: jungschi-back
  ports:
    - port: 8080

